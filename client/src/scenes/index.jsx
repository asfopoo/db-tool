import React, {
  Component,
} from 'react';

import {
  Switch,
  Route,
} from 'react-router-dom';

import {
  withLoadUser,
} from 'auth';

class Routes extends Component {
  render(){

  }
}
export default withLoadUser(Routes);
